"use strict";
// // Tuples is an array with some restrictions on it. Elements in the array must follow each other in a specific order
// let user: [string, number, boolean];
// user = ["Franc", 10, false]; //elements must follow in the order specified in line 3
// // Note that array methods like push and pop will still apply to tuples
